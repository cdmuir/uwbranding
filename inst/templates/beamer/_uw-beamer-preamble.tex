% ============================================================
% UW–Madison Brand Identity — Beamer Preamble
% XeLaTeX required (fontspec for Red Hat Display/Text)
%
% LIGHT/DARK TOGGLE — change one line:
%   \uwdarkfalse   → white background, Badger Red frametitle
%   \uwdarktrue    → near-black background, Badger Red accents
% ============================================================

% ------------------------------------------------------------
% Packages
% ------------------------------------------------------------
\usepackage{xcolor}
\usepackage{fontspec}
\usepackage{graphicx}

% ------------------------------------------------------------
% Brand Colors
% ------------------------------------------------------------
\definecolor{BadgerRed}{HTML}{C5050C}
\definecolor{DarkRed}{HTML}{9B0000}
\definecolor{LightGray}{HTML}{E1E5E7}
\definecolor{UWBlack}{HTML}{121212}
\definecolor{UWYellow}{HTML}{FFBF00}

% ------------------------------------------------------------
% Light / Dark Toggle
% ------------------------------------------------------------
\newif\ifuwdark
\uwdarkfalse   % ← change to \uwdarktrue for dark mode
% \uwdarktrue

% ------------------------------------------------------------
% Fonts (XeLaTeX / fontspec)
%   Beamer defaults to \sffamily; map that to Red Hat Display.
%   Red Hat Text is available via \RedHatText for body emphasis.
% ------------------------------------------------------------
\setsansfont{Red Hat Display}[
  UprightFont    = * Regular,
  BoldFont       = * Bold,
  ItalicFont     = * Italic,
  BoldItalicFont = * Bold Italic,
]
\setmainfont{Red Hat Text}[
  UprightFont    = * Regular,
  BoldFont       = * Bold,
  ItalicFont     = * Italic,
  BoldItalicFont = * Bold Italic,
]

% ------------------------------------------------------------
% Base Beamer Theme
% ------------------------------------------------------------
\usetheme{default}
\setbeamertemplate{navigation symbols}{}   % remove nav icons

% ------------------------------------------------------------
% Color Themes
% ------------------------------------------------------------
\ifuwdark
  % ── Dark mode ──────────────────────────────────────────────
  \setbeamercolor{normal text}      {fg=LightGray,  bg=UWBlack}
  \setbeamercolor{background canvas}{bg=UWBlack}
  \setbeamercolor{structure}        {fg=BadgerRed}
  \setbeamercolor{frametitle}       {fg=white,      bg=BadgerRed}
  \setbeamercolor{framesubtitle}    {fg=LightGray}
  \setbeamercolor{title}            {fg=white,      bg=BadgerRed}
  \setbeamercolor{subtitle}         {fg=LightGray}
  \setbeamercolor{author}           {fg=LightGray}
  \setbeamercolor{date}             {fg=LightGray}
  \setbeamercolor{institute}        {fg=LightGray}
  \setbeamercolor{alerted text}     {fg=BadgerRed}
  \setbeamercolor{example text}     {fg=UWYellow}
  \setbeamercolor{itemize item}     {fg=BadgerRed}
  \setbeamercolor{itemize subitem}  {fg=DarkRed}
  \setbeamercolor{enumerate item}   {fg=BadgerRed}
  \setbeamercolor{block title}      {fg=white,      bg=BadgerRed}
  \setbeamercolor{block body}       {fg=LightGray,  bg=UWBlack!85!white}
  \setbeamercolor{block title alerted}{fg=white,    bg=DarkRed}
  \setbeamercolor{block body alerted}{fg=LightGray, bg=UWBlack!85!white}
  \setbeamercolor{footline}         {fg=LightGray!60!UWBlack, bg=UWBlack}
\else
  % ── Light mode ─────────────────────────────────────────────
  \setbeamercolor{normal text}      {fg=UWBlack,    bg=white}
  \setbeamercolor{background canvas}{bg=white}
  \setbeamercolor{structure}        {fg=BadgerRed}
  \setbeamercolor{frametitle}       {fg=white,      bg=BadgerRed}
  \setbeamercolor{framesubtitle}    {fg=UWBlack}
  \setbeamercolor{title}            {fg=white,      bg=BadgerRed}
  \setbeamercolor{subtitle}         {fg=LightGray}
  \setbeamercolor{author}           {fg=LightGray}
  \setbeamercolor{date}             {fg=LightGray}
  \setbeamercolor{institute}        {fg=LightGray}
  \setbeamercolor{alerted text}     {fg=BadgerRed}
  \setbeamercolor{example text}     {fg=DarkRed}
  \setbeamercolor{itemize item}     {fg=BadgerRed}
  \setbeamercolor{itemize subitem}  {fg=DarkRed}
  \setbeamercolor{enumerate item}   {fg=BadgerRed}
  \setbeamercolor{block title}      {fg=white,      bg=BadgerRed}
  \setbeamercolor{block body}       {fg=UWBlack,    bg=LightGray}
  \setbeamercolor{block title alerted}{fg=white,    bg=DarkRed}
  \setbeamercolor{block body alerted}{fg=UWBlack,   bg=LightGray}
  \setbeamercolor{footline}         {fg=UWBlack!55, bg=LightGray}
\fi

% ------------------------------------------------------------
% Frame Title
% ------------------------------------------------------------
\setbeamertemplate{frametitle}{%
  \nointerlineskip%
  \begin{beamercolorbox}[wd=\paperwidth, leftskip=0.06\paperwidth,
      ht=2.2em, dp=0.5em]{frametitle}%
    \usebeamerfont{frametitle}\insertframetitle%
    \ifx\insertframesubtitle\@empty\else%
      \\[0.15em]%
      {\usebeamerfont{framesubtitle}\usebeamercolor[fg]{framesubtitle}%
       \insertframesubtitle}%
    \fi%
  \end{beamercolorbox}%
}

% ------------------------------------------------------------
% Title Page
%   Badger Red upper panel — same look in both light and dark.
% ------------------------------------------------------------
\defbeamertemplate*{title page}{uwmadison}{%
  \begin{beamercolorbox}[wd=\paperwidth, ht=0.45\paperheight,
      dp=0pt, leftskip=0.06\paperwidth]{title}%
    \vfill
    {\usebeamerfont{title}\inserttitle\par}%
    \ifx\insertsubtitle\@empty\else%
      \vspace{0.4em}%
      {\usebeamerfont{subtitle}\usebeamercolor[fg]{subtitle}%
       \insertsubtitle\par}%
    \fi%
    \vfill
  \end{beamercolorbox}%
  \begin{beamercolorbox}[wd=\paperwidth, ht=0.55\paperheight,
      dp=0pt, leftskip=0.06\paperwidth]{background canvas}%
    \vspace{1.6em}%
    {\usebeamerfont{author}\usebeamercolor[fg]{author}\insertauthor\par}%
    \vspace{0.3em}%
    {\usebeamerfont{institute}\usebeamercolor[fg]{institute}\insertinstitute\par}%
    \vspace{0.3em}%
    {\usebeamerfont{date}\usebeamercolor[fg]{date}\insertdate\par}%
    \vfill
  \end{beamercolorbox}%
}

% ------------------------------------------------------------
% Bullet Points
% ------------------------------------------------------------
\setbeamertemplate{itemize item}  {\color{BadgerRed}\small$\blacksquare$}
\setbeamertemplate{itemize subitem}{\color{DarkRed}\small$\square$}
\setbeamertemplate{itemize subsubitem}{\color{DarkRed}--}

% ------------------------------------------------------------
% Footer — slide number only (clean, minimal)
% ------------------------------------------------------------
\setbeamertemplate{footline}{%
  \usebeamercolor[fg]{footline}%
  \hfill%
  {\tiny\insertframenumber\,/\,\inserttotalframenumber\hspace{1em}}%
  \vspace{0.5em}%
}

% ------------------------------------------------------------
% Fonts (Beamer-specific sizes)
% ------------------------------------------------------------
\setbeamerfont{title}        {size=\Large,  series=\bfseries, family=\sffamily}
\setbeamerfont{subtitle}     {size=\normalsize, series=\normalfont}
\setbeamerfont{frametitle}   {size=\large,  series=\bfseries, family=\sffamily}
\setbeamerfont{framesubtitle}{size=\small,  series=\normalfont}
\setbeamerfont{author}       {size=\small,  series=\normalfont}
\setbeamerfont{institute}    {size=\small,  series=\normalfont}
\setbeamerfont{date}         {size=\small,  series=\normalfont}
\setbeamerfont{block title}  {size=\normalsize, series=\bfseries, family=\sffamily}

% ------------------------------------------------------------
% Convenience Environments
% ------------------------------------------------------------

% \begin{uwbox}{Title} ... \end{uwbox}
%   Same as Beamer block but always uses BadgerRed/brand colors.
\newenvironment{uwbox}[1]{%
  \begin{block}{#1}%
}{%
  \end{block}%
}
