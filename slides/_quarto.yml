project:
  type: default
  output-dir: ../_output/slides   # rendered slides land alongside PDF output

# Slide formats are specified per-file. Only shared execution
# defaults live here so the root _quarto.yml PDF/XeLaTeX settings
# don't bleed into RevealJS or Beamer files.
execute:
  echo: false
  warning: false
