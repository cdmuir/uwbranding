# theme_uw() grid argument is validated

    Code
      theme_uw(grid = "diagonal")
    Condition
      Error in `match.arg()`:
      ! 'arg' should be one of "both", "x", "y", "none"

# theme_uw_dark() style argument is validated

    Code
      theme_uw_dark(style = "pink")
    Condition
      Error in `match.arg()`:
      ! 'arg' should be one of "black", "red"

