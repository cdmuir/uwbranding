% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_uw.R
\name{theme_uw}
\alias{theme_uw}
\title{UW–Madison ggplot2 theme (light)}
\usage{
theme_uw(
  base_size = 11,
  base_family = "Crimson Pro",
  title_color = "badger_red",
  grid = c("both", "x", "y", "none"),
  ...
)
}
\arguments{
\item{base_size}{Base font size in points. Default \code{11}.}

\item{base_family}{Base font family for body text. Default
\code{"Crimson Pro"}. Use \code{"Red Hat Text"} for a pure sans-serif body.}

\item{title_color}{Color for the plot title. Accepts a name from
\link{uw_colors} or any hex string. Default \code{"badger_red"}.}

\item{grid}{Which major grid lines to show. One of \code{"both"} (default),
\code{"x"}, \code{"y"}, or \code{"none"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Applies the UW–Madison brand identity to a ggplot2 plot:
\itemize{
\item Red Hat Display for titles and facet strip labels
\item Red Hat Text for axis labels, legend text, and captions
\item Crimson Pro (or \code{base_family}) for all other text
\item Badger Red title and facet strip background
\item Light gray grid lines on a white panel
}
}
\examples{
ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point() +
  ggplot2::labs(title = "Fuel Economy") +
  theme_uw()

# Grid on x axis only, larger base size
ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point() +
  theme_uw(base_size = 14, grid = "x")
}
