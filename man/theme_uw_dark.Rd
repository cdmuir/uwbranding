% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_uw_dark.R
\name{theme_uw_dark}
\alias{theme_uw_dark}
\title{UW–Madison ggplot2 theme (dark)}
\usage{
theme_uw_dark(
  style = c("black", "red"),
  base_size = 14,
  base_family = "Red Hat Text",
  grid = NULL,
  ...
)
}
\arguments{
\item{style}{Background style. \code{"black"} (default) uses a near-black
(\verb{#121212}) background with a Badger Red title. \code{"red"} uses a Badger
Red background with a white title.}

\item{base_size}{Base font size in points. Default \code{14} — larger than
\code{\link[=theme_uw]{theme_uw()}} because dark themes are typically used at presentation
scale.}

\item{base_family}{Base font family. Default \code{"Red Hat Text"} (sans-serif
reads better on screen at presentation sizes).}

\item{grid}{Which major grid lines to show. One of \code{"both"}, \code{"x"},
\code{"y"}, or \code{"none"}. Defaults to \code{"both"} for \code{style = "black"} and
\code{"none"} for \code{style = "red"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Two background styles for slides and poster panels, both using light text
with Badger Red accents. The title slide treatment matches the Quarto
RevealJS and Beamer templates.
}
\examples{
ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point(color = "white") +
  theme_uw_dark()

ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point(color = "white") +
  theme_uw_dark(style = "red")
}
